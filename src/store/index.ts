import Vue from 'vue'
import Vuex from 'vuex'
import EventsData from './modules/event-data'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    EventsData
  }
})
